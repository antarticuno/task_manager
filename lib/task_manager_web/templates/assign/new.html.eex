<h1>New Assign</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.assign_path(@conn, :create)) %>

<span><%= link "View all Tasks", to: Routes.task_path(@conn, :index) %></span>
<br />
<span><%= link "Back to Assignments", to: Routes.assign_path(@conn, :index) %></span>
